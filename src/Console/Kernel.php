<?php

namespace Dhayakawa\SpringIntoAction\Console;

use Illuminate\Console\Scheduling\Schedule;
use Illuminate\Foundation\Console\Kernel as ConsoleKernel;

class Kernel extends ConsoleKernel
{
    /**
     * The Artisan commands provided by your application.
     *
     * @var array
     */
    protected $commands = [
        //
    ];

    /**
     * Define the application's command schedule.
     *
     * @param  Schedule  $schedule
     * @return void
     */
    protected function schedule(Schedule $schedule)
    {
        $schedule->command('grove:import individuals')->hourlyAt(01)->emailOutputOnFailure('david.hayakawa@gmail.com');
        $schedule->command('grove:import family')->hourlyAt(15)->emailOutputOnFailure('david.hayakawa@gmail.com');
        $schedule->command('grove:import lifegroup')->dailyAt('24:00')->emailOutputOnFailure('david.hayakawa@gmail.com');
    }

    /**
     * Register the commands for the application.
     *
     * @return void
     */
    protected function commands()
    {
        $this->load(__DIR__.'/Commands');

        require base_path('routes/console.php');
    }

    /**
     * Get the timezone that should be used by default for scheduled events.
     *
     * @return \DateTimeZone|string|null
     */
    protected function scheduleTimezone()
    {
        return 'America/Chicago';
    }
}
